import { LoginHellojsDirective } from './login-hellojs.directive';

describe('LoginHellojsDirective', () => {
  it('should create an instance', () => {
    const directive = new LoginHellojsDirective();
    expect(directive).toBeTruthy();
  });
});
