import { LogoutHellojsDirective } from './logout-hellojs.directive';

describe('LogoutHellojsDirective', () => {
  it('should create an instance', () => {
    const directive = new LogoutHellojsDirective();
    expect(directive).toBeTruthy();
  });
});
